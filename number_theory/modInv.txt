int modExp(int x,int n, int m)
{
    if(n==0)
    return 1;
    else if(n%2==0)
    return modExp((x*x)%m,n/2,m);
    else 
    return (x*modExp((x*x)%m,(n-1)/2,m))%m;
}
//works if m is prime,
//uses Fermat Little Theorem

int modInv(int n,int m)
{
    return modExp(A,M-2,M);
}